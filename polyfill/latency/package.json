{
  "name": "@frontend-bindings/polyfill-latency",
  "version": "1.0.0",
  "description": "Write modern asynchronous sleep, export for older browsers",
  "license": "UNLICENSED",
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "build": "use-run-all build:*",
    "build:ie6to8": "cross-env BROWSERSLIST='ie 6-8' use-bili --target browser --format umd --module-name latency  --file-name ie6.js --verbose",
    "build:main": "use-bili --target browser --format cjs --file-name index.js",
    "clean": "use-rimraf .rpt2_cache dist *.*.log",
    "fix": "just-scripts fix",
    "just-scripts": "just-scripts",
    "lint": "just-scripts lint",
    "lint:fix": "just-scripts lint --fix",
    "test": "just-scripts test",
    "test:snapshots": "just-scripts test --snapshots"
  },
  "prettier": "@frontend-bindings/conventions-use-prettier",
  "dependencies": {
    "@babel/preset-env": "^7.4.0",
    "@babel/runtime-corejs3": "~7.5.5"
  },
  "devDependencies": {
    "@frontend-bindings/conventions-use-bili": "~1.0.0",
    "@frontend-bindings/conventions-use-eslint": "~1.0.0",
    "@frontend-bindings/conventions-use-jest": "~1.0.0",
    "@frontend-bindings/conventions-use-prettier": "~1.0.0",
    "@frontend-bindings/conventions-use-typescript-3": "~1.0.0",
    "@frontend-bindings/tools-bundling-helpers": "~1.0.0",
    "cross-env": "^5.2.0",
    "@types/jest": "^24.0.15",
    "@types/node": "^10.14.17",
    "core-js": "^3.1.4",
    "just-scripts": "~0.28.2"
  }
}
