import { CodeFormatterCommandLineParser } from './command-line-parser'

import type {
  INormalizedCliArguments,
  WrappedCommandsType,
  Options,
  RequiredOptions,
} from './types'

export type {
  INormalizedCliArguments,
  WrappedCommandsType,
  Options,
  RequiredOptions,
}

export { CodeFormatterCommandLineParser }

export default CodeFormatterCommandLineParser
